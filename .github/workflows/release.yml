name: Build, Release, and Publish

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build and Release
    runs-on: windows-latest

    steps:
      # 1. Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Debug: Verify package-lock.json
      - name: Verify package-lock.json
        run: ls -la cli/

      # 3. Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'
          cache-dependency-path: cli/package-lock.json

      # 4. Install Node.js dependencies
      - name: Install Node.js dependencies
        working-directory: cli
        run: npm ci

      # 5. Set up Go
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.23"

      # 6. Build Go binary
      - name: Build Windows Binary
        run: go build -o bin/gapizer.exe

      # 7. Install Inno Setup (Windows installer)
      - name: Install Inno Setup
        run: choco install innosetup --yes

      # 8. Build Windows Installer
      - name: Build Windows Installer
        run: |
          "& 'C:\Program Files (x86)\Inno Setup 6\ISCC.exe' installer/gapizer_installer.iss"

      # 9. Debug Paths (optional, for debugging purposes)
      - name: Debug Paths
        run: |
          echo "Current Directory:"
          pwd
          echo "Listing Output Directory:"
          dir Output

      # 10. Upload Installer as Artifact
      - name: Upload Installer as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: gapizer-installer
          path: Output\GAPIzer-Installer.exe

      # 11. Run semantic-release for versioning and npm publish
      - name: Run semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx semantic-release
